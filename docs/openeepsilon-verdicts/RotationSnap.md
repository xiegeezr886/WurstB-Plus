状态：不适用

## 对照证据

参考侧没有对应模块。本项为自审，全文读过 `hacks/RotationSnapHack.java`（106 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 取整算法 | `:62-69`（yaw）、`:74-83`（pitch）`Math.floorMod` 取余后按"余数是否过半"决定进/退 | 正确（`floorMod` 保证余数非负，负角度（-180~0）也能正确吸附；直接 `%` 会在负角度出错） |
| 区间 | `:86-91` 15°/30°/45°/90° | 正常 |
| 俯仰限制 | `:81-82` 吸附后 `Mth.clamp(-90, 90)` | 正确（吸附可能把 89° 推到 90°，仍在合法范围） |
| 开关 | `:46-55` 注册/摘除 `UpdateListener` | 平衡 |

无改动。

## 建议（未做）

- 吸附是每 tick 直接改写 `setYRot`/`setXRot`，因此鼠标移动会被"拽"回刻度（这是该模块的设计），
  但和需要精确瞄准的模块（KillAura 的 silent rotation、AutoAim）同时使用时会把它们的角度一起吸附掉。
  没有互斥组也没有文档提示；加互斥会改变现有行为，未做，只记录。
