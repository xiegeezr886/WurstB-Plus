状态：不适用

## 对照证据

参考侧没有对应模块（参考没有自动签名）。本项为自审，全文读过 `hacks/AutoSignHack.java`（43 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 记录文本 | `:38-42` `setSignText` 只在"模块启用 + 还没记录过"时保存 | 正确（第一次签名时记下内容，后续沿用） |
| 读取 | `:33-36` `getSignText()` 供 `AbstractSignEditScreenMixin` 在打开牌子界面时套用 | 正常 |
| 关闭清理 | `:27-31` `onDisable` 把 `signText` 置空 | 正确（下次开启重新记录，不会用上一次的内容） |
| 不存档 | `:16` `@DontSaveState` | 合理（签名内容不该写进配置） |

无改动。
