状态：不适用

## 对照证据

参考侧没有对应模块。本项为自审，全文读过 `hacks/AirJumpHack.java`（101 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 判定 | `:69-74` 把 10 个状态（是否按下边沿、是否落地、模式、双跳是否可用、旁观/骑乘/飞行/鞘翅/水中/梯子）交给纯类 `util/AirJumpPolicy.canJump()` | 正常（策略类可单测，mixin/模块只负责取状态） |
| 按键边沿 | `:64-66` `pressedEdge = 按下 && 上一 tick 没按` | 正确（不然按住空格会无限跳） |
| 跳完的状态同步 | `:76-80` `setOnGround(false)`，双跳模式下消耗掉机会 | 正确（避免同一 tick 被原版再判定一次落地） |
| 换世界/重生 | `:56-60` `player == null` 时同步按键状态 | 正常 |
| 关闭 | `:44-50` 摘监听并复位内部状态 | 正常 |

无改动（本行此前在覆盖表里是"待办"，代码已经是抽出 `AirJumpPolicy` 的新写法，属重复列出）。

## 建议（未做）

- `SearchTags` 与 `Jetpack` 重叠（都含 `"air jump"`），且 Jetpack 的 tag 里也有 `"AirJump"`；
  未改。
