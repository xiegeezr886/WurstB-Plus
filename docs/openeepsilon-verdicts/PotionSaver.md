状态：不适用

## 对照证据

参考侧没有对应模块（参考的 `misc/XCarry.kt` 是允许在界面里整理背包，与"冻结移动包省药水"无关）。
本项为自审，全文读过 `hacks/PotionSaverHack.java`（53 行）。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 冻结条件 | `:47-52` 有任意药水效果 + 水平速度恰好为 0 | 正常（这就是"挂机时药水不流逝"的原理） |
| 取消的包 | `:43-44` 只取消 `ServerboundMovePlayerPacket` | 范围正确（不会误伤交互/聊天包） |
| 关闭时恢复 | `:31-35` 摘监听，从此不再拦包 | 正常 |

无改动。

## 建议（未做）

- `:51` 只判断 `x`/`z` 速度为 0，没判断 `y`：玩家**正在下落或上浮**（y ≠ 0）时也会拦掉移动包，
  服务端不会得知坐标变化，反作弊视角下是典型的"位置不同步"特征；对药水保存本身没有影响。
  属于该模块的固有取舍，未改。
- 同样因为拦包，服务器端位置会与客户端持续偏离，长期挂机后恢复移动时可能出现回弹；未改。
