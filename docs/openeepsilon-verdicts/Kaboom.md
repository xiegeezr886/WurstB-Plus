状态：不适用

## 对照证据

参考侧没有对应模块（参考的 `player/Kaboom.kt`? 实际参考里没有这个模块）。
本项为自审，全文读过 `hacks/KaboomHack.java`（109 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 防踢保护 | `:67-69` 非创造且不在落地状态时直接返回 | 正确（这个模块会一次发极多破坏包，必须限制） |
| 特效 | `:76-85` 本地爆炸音效 + `EXPLOSION_EMITTER` 粒子 | 正常（纯视觉/听觉，不发包） |
| 破坏 | `:88-90` 取 6 格内、按距离**从远到近**排序的方块，循环 `power` 次调用 `BlockBreaker.breakBlocksWithPacketSpam` | 从远到近是为了让"最近的方块最后被破坏"，减少自己被卡住/被拉回；`power` 就是"发包次数"（默认 128，可配 32~512），与设置说明一致 |
| 一次性 | `:92` 关闭模块 | 正确 |
| 关闭 | `:58-62` 摘监听 | 平衡 |

无改动。

## 建议（未做）

- 默认 `power = 128`（最大 512）会在一 tick 内发出上千个破坏包，被反作弊判定几乎是必然的；
  设置说明里已提示风险，未改默认值。
