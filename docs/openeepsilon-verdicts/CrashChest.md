状态：不适用

## 对照证据

参考侧没有 ITEMS 类模块；与"制造异常物品"最接近的是 `misc/Crasher.kt`（发畸形包让服务器崩），
机制完全不同（本模块是造一个带 40000 条空列表 NBT 的箱子物品，用于让查看者客户端卡死/崩溃）。
本项为自审。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 前置检查 | `hacks/CrashChestHack.java:33-45`：必须创造模式、鞋子栏必须为空，否则报错并 `setEnabled(false)` **且 `return`** | 正确（与 `ItemGeneratorHack` 旧实现形成对照，那个漏了 return，已单独修） |
| NBT 构造 | `:48-54` 40000 个空 `ListTag` 塞进 `www.wurstclient.net` 键 | 是 40000 个**同级**元素而不是递归嵌套，构造本身不会栈溢出 |
| 命名 | `:55` `setHoverName(Component.literal("复制我"))` | 正常 |
| 放入鞋子栏 | `:58` `inventory.armor.set(0, stack)`，随后自动关闭 | 仅客户端伪造，服务端不认；这正是该模块的用法（创造模式演示/恶作剧） |

无改动。

## 建议（未做）

- 40000 条 NBT 会让物品包体积很大，发送时可能触发服务端的包大小限制被踢；这是模块用途的一部分，
  未加限制或提示。
- `net.minecraft.nbt` 的 `ItemStack#setTag` 在 1.20.5+ 已被数据组件取代；本文件属于 1.20.1 源集，
  其它版本的源集各自有实现，未跨版本统一。
