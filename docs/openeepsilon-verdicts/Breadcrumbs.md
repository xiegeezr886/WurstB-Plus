状态：不适用

## 对照证据

参考侧有 `render/Trail.kt`（1.12.2 的移动轨迹）。本工程 `hacks/BreadcrumbsHack.java`（149 行）
用 `DEBUG_LINE_STRIP` 画带透明度渐变的轨迹。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 采样 | `:87-91` 每 tick 记录、位移 > 0.1 才存点（避免站着不动刷点） | 正确 |
| 上限 | `:93-95` 超过 `Max points` 从头丢 | 正确（默认 500，滑块 50~5000） |
| 换世界/维度 | `:74-79` `onWorldChange` 清空并重置 `lastPos` | 正确（否则会跨维度连线） |
| 关闭 | `:64-72` 摘三个监听器 + 清空列表 | 正确 |
| GL 状态 | `:111-147` 开了 `blend/depthMask/lineWidth`，在 `finally` 里全部还原 | 正确（渲染模块里最容易漏的一环） |
| 空值保护 | `:84-85`、`:101-102` | 正确 |

无改动（本文件没有 GPL 头注释）。

## 建议（未做）

- `points` 是 `ArrayList`，上限 5000 时 `remove(0)` 是 O(n)；轨迹点每 tick 最多加一个，
  实际开销可忽略。未改。
- 关掉模块会丢掉已有轨迹（`:70` 清空），重开从空开始。这是设计选择（参考也是每帧重算），只记录。
