状态：不适用

## 对照证据

参考侧没有对应模块（`render/AntiOverlay.kt` / `render/NoRender.kt` 是屏蔽原版叠加层，
不是"加一层全屏着色器"）。本项为自审。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 着色器资源 | `hacks/LsdHack.java:19-20` 指向 `wurst:shaders/post/lsd_wobble.json`；两个文件（post 与 program）确实存在 | 不会加载失败 |
| 启用时处理已有特效 | `:37-40` 先 `shutdownEffect()` 再 `loadEffect(EFFECT)` | 正常 |
| 关闭时清理 | `:43-48` 关闭特效 | 正常 |
| 未存档 | `:16` `@DontSaveState` | 合理（一次性视觉特效） |
| 摄像机检查 | `:31-35` 相机实体不是玩家就自动关闭 | 正常（旁观/自由视角下着色器会怪掉） |

无改动。

## 建议（未做）

- `onDisable` 只会关掉当前特效，不会恢复启用之前存在的特效（如果以后别的功能也加载 post effect，
  需要改成记录并还原）。目前工程里只有本模块加载特效，未改。
- 着色器内容（扭曲强度、速度）是写死的，没有设置项；参考侧没有等价模块可对照，未改。
