状态：不适用

## 对照证据

参考侧没有对应模块。本项为自审，全文读过 `hacks/FishHack.java`（49 行）。

与 `Dolphin` 是同一实现的两档（增量 0.005 vs 0.04），`onEnable` 里互相关闭，`SearchTags` 也都是
`{"AutoSwim", "auto swim"}`。

无改动。

## 建议（未做）

- 两个模块的实现几乎逐行相同（只差一个常量），工程里还存在 `AutoSwim`（靠疾跑加速）——
  三者功能重叠。合并成一个带滑条的模块更合理，但那会改变用户可见的模块列表与设置，
  属于重构决策，本次只记录。
