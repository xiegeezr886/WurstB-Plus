状态：不适用

## 对照证据

参考侧没有对应模块（参考没有"按种子预测结构位置并透视"）。本项为**结构审计 + 关键区段核对**
（文件 265 行，属另一路并行开发维护的 `seed/**` 相关模块，本次只审计不修改）。

结构（方法清单）：`collectSurfaceBlocks` / `showNothing` / `closeBuffer` / `rebuildBuffer`。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 事件对称 | `:98-99` 添加、`:107-108` 移除（Update/Render 各一） | 平衡 |
| 关闭清理 | `:105-114` 摘监听 + `finder.clear()` + `shown.clear()` + `closeBuffer()` | 完整（顶点缓冲不会泄漏） |
| 启用初始化 | `:93-104` 复位计时器 + 提示当前种子来源（`finder.describeSources()`） | 正确（用户能看到"数据来自哪个种子"） |

无改动。

## 未做 / 边界

- 未逐行审计 `finder`（结构定位）的实现，只核对了本模块的事件与生命周期。
- 没有实机验证渲染结果。
