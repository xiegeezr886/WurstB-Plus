状态：不适用

## 对照证据

参考侧没有对应模块。本项为自审（模块全文 24 行，是标记式功能）。

## 自审记录

标记模块，实际逻辑在下界传送门导致的反胃（nausea）处理里。已核实调用点：

| 调用点 | 位置 | 行为 |
| --- | --- | --- |
| 传送门内开界面 | `mixin/ClientPlayerEntityMixin.java:198` `if(!portalGuiHack.isEnabled()) return;` 之后跳过原版强制关界面的逻辑 | 开启后可以在传送门里开背包/交易界面 |

无改动。

## 建议（未做）

- 原版在传送门里会强制关闭容器界面（`LocalPlayer.java` 的 nausea 处理），本模块只跳过"关闭"这一步；
  传送动作本身仍会发生，因此**界面会在传送过程中被原版关掉**这件事需要实机确认，本次只审计未改。
