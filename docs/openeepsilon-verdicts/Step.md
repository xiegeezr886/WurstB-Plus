状态：不适用

## 对照证据

参考侧有 `movement/Step.kt`。本工程 `hacks/StepHack.java`（161 行）分 Simple/Legit 两档。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| Simple | `:71-75` 直接改 `player.maxUpStep`（1..5 格） | 正常 |
| Legit | `:77-119` 先发两个抬升包（0.42/0.753 倍步高）再 `setPos` 抬升，配 2 tick 冷却 | 1.8~1.12 时代流传的 NCP 绕过写法，1.20 服务端仍按位置包处理 |
| 前置条件 | `:79-104` 需要水平碰撞、落地、不在梯子/水/岩浆、有移动输入、没按跳跃，且无碰撞箱、步高在 (0.5, 1] | 逐条核对，没有明显漏洞 |
| 换世界/重生 | `:121-136` `trackPlayer`/`restoreTrackedPlayer` 记录并还原 `maxUpStep` | 正确（不会把旧玩家的值写回新玩家） |
| 关闭 | `:54-59` 还原 `maxUpStep` | 正确 |
| 与 auto-jump 的配合 | `:138-141` `isAutoJumpAllowed()` 供 mixin 使用 | 正常 |

无改动（本行此前在覆盖表里是"待办"，代码已经是新写法，属重复列出）。

## 建议（未做）

- Legit 模式的两个位置包与 `setPos` 之间的时序（同一 tick 内先发包再改本地坐标）无法在本工程内验证，
  需要实机 + 服务端插件才能确认是否仍能绕过；本次只做静态审计，未改。
