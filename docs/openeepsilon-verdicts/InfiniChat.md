状态：不适用

## 对照证据

参考侧没有 CHAT 类模块，也没有"无限聊天"这种功能（参考的长聊天输入是原版/GUI 层的事）。

## 自审记录

- 模块本身只是一个开关标记：`hacks/InfiniChatHack.java` 全文 22 行，没有设置项、不监听任何事件。
- 真正的实现挂在 mixin 上：`mixin/ChatScreenMixin.java:38` 读 `getHax().infiniChatHack.isEnabled()`
  （原版聊天输入框有 100 字符上限与历史记录上限，这里按开关放宽）。
- 因此"参考实现缺失"不影响本模块——它没有可优化的自有逻辑，唯一能改的地方在 mixin 里，
  而那段代码不在本次对照范围内（属于渲染/GUI 层）。

无改动。

## 建议（未做）

- 如果以后要把它做成可配置（例如自定义长度上限），应该把上限放进设置项而不是写死在 mixin 里；
  目前没有需求，未改。
