状态：不适用

## 对照证据

参考侧没有对应模块（Prophunt 是服务器玩法）。本项为自审，全文读过
`hacks/ProphuntEspHack.java`（73 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 判定 | `:56-68` 只画 `Mob` 类型的隐身实体（Prophunt 里玩家伪装成方块后服务端会把玩家换成假实体，客户端表现为隐身的 Mob） | 与场景一致 |
| 排除自己 | `:64-65` 距离 < 0.25 的跳过（含自己） | 正确 |
| 闪烁 | `:50-52` 用 `System.currentTimeMillis()` 的 1 秒周期做呼吸透明度 | 正常（纯视觉） |
| 事件 | `:34-44` 注册/摘除 | 平衡 |

无改动。

## 关于"参考侧没有"的说明

参考的 `render/EntityESP.kt` 是通用实体透视，不处理"隐身的假方块"，因此没有可搬运的实现。
