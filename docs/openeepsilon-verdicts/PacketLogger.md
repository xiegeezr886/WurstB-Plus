状态：不适用

## 对照证据

参考侧没有对应模块。本项为自审，全文读过 `hacks/PacketLoggerHack.java`（91 行）。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 收发分别开关 | `:23-26,58-72` `Log received` / `Log sent` | 正常 |
| 限流 | `:74-84` 超过 100 条后丢弃，直到满 1 秒才重置 | 正常（每包两行聊天栏的输出量必须有上限） |
| 数据详情 | `:27-28,87-88` `Show data` 默认关，打开后附加 `packet.toString()` | 合理 |
| 关闭时摘监听 | `:51-56` 两个监听都移除 | 正常 |

无改动。

## 建议（未做）

- `ChatUtils.message()` 是直接往聊天栏加消息（`MC.gui.getChat().addMessage`）。
  收包回调在客户端主线程上跑没问题，但**发包**回调 `PacketOutputEvent` 由调用方线程触发，
  某些路径可能在网络线程上；这一点本次没有构造出实际出错的反例（需要实机抓线程），
  只记录：如果以后出现随机崩溃/聊天栏错乱，优先怀疑这里。
- `:86` 只用 `getSimpleName()`，不同包的类名可能重名（例如 `Packet` 匿名子类）时无法区分；
  属显示取舍，未改。
