状态：不适用

## 对照证据

参考侧的 `Timer` 是一个**给别的模块用**的 tick 长度覆盖栈（`TimerManager`，带超时、按插入顺序取最后一个）。
本工程没有任何模块需要"临时改几个 tick 的 tick 长度"这种动作，所以参考那套抽象没有消费方 ——
与之前判定不做 `util/pause/*` 令牌是同一个理由（凭空抽象）。

本工程 `hacks/TimerHack.java` 是另一种东西：用户直接设定整体速度倍率。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 速度范围 | `:18-19` 0.1..100，默认 2 | 正常 |
| 关闭即恢复 | `:34-37` `getTimerSpeed()` 在模块关闭时返回 1 | 正常（不会留下加速状态） |
| 显示 | `:29-32` 渲染名里带当前倍率 | 正常 |
| 落地方式 | 由 mixin 读 `getTimerSpeed()` 覆盖 tick 长度（`See` 注释所指） | 正常 |

无改动。

## 建议（未做）

- 覆盖表里这一行与常用 hack 表的结论重复（常用表里 `Timer` 早已是"不适用（§0.3）"：
  参考的 `TimerManager` 是给别的模块用的覆盖栈，本工程没有消费方）。本次给出一致结论。
