状态：不适用

## 对照证据

参考侧没有对应模块（参考的 `FastSwim` 是水平加速）。本项为自审，全文读过
`hacks/AutoSwimHack.java`（51 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 条件 | `:42-46` 没有水平碰撞、没按潜行、在水中 | 正常 |
| 效果 | `:48-49` `player.zza > 0` 时 `setSprinting(true)` | 正常（`zza` 是原版的前进输入量，1 tick 的滞后对游泳加速没有影响） |
| 关闭 | `:31-35` 摘监听；疾跑状态由原版在条件不满足时自行结束 | 正常 |

无改动。

## 建议（未做）

- `player.zza` 在本工程里同时被 `NoSlowdown`/`AutoSprint` 等模块间接影响；`zza` 在
  UpdateEvent 时点是上一 tick 的值（原版在 `aiStep()` 里才更新），对"是否在前进"的判定有一 tick 滞后。
  没有构造出可触发的反例，只记录。
