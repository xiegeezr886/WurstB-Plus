状态：不适用

## 对照证据

参考侧没有对应模块（参考的 `Velocity.kt` 是取消自己受到的击退，与"不被实体挤开"不同）。
本项为自审，全文读过 `hacks/AntiEntityPushHack.java`（45 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 事件 | `:38-44` `VelocityFromEntityCollisionListener`，只在"被挤的实体是玩家自己"时取消 | 正确（不会影响其它实体互相推挤） |
| 开关 | `:26-36` 增减监听器 | 正常 |

无改动。

## 建议（未做）

- 与 `AntiWaterPush`（水流推力）、`Velocity`（受击击退）是三个不同来源，各管一段；
  没有互相冲突。未改。
