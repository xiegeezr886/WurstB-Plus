状态：不适用

## 对照证据

参考侧没有对应模块（参考没有"围绕目标的爆破点高亮"）。本项为自审，全文读过
`hacks/CityEspHack.java`（150 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 扫描频率 | `:84-87` 每 10 tick 扫一次 | 正确（实体搜索不便宜） |
| 目标筛选 | `:92-103` 排除已移除、死亡、自己、假玩家；`Ignore friends`、`Players only`、范围过滤；按距离排序 | 逐个核对，与设置说明一致 |
| 判定方块 | `:124-128` 黑曜石/哭泣的黑曜石；`:117-118` 排除不可破坏方块 | 正确 |
| 缓存 | `:52,68,78,89` 列表在开关与每次扫描时清空 | 正确（关掉后不残留旧框） |
| 颜色 | `:133-137` 由 `ColorSetting` 拆 RGB，填充 0.25/描边 0.6 | 正常 |

无改动（本文件没有 GPL 头注释）。

## 说明

- `:147` 的 `record CityBlock(BlockPos pos, Entity target)` 里 `target` 字段目前只在构造时写入、
  渲染时没用（按位置画框）。它保留了"这个爆点属于哪个目标"的信息，删掉会改数据流；未动，只记录。
