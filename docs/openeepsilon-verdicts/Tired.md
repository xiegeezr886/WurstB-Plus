状态：不适用

## 对照证据

参考侧没有对应模块。本项为自审。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 波形 | `hacks/TiredHack.java:42`：pitch = `tickCount % 100`，慢速抬头 | 见建议 |
| 发包方式 | `:42-43` `new Rotation(getYRot(), pitch).sendPlayerLookPacket()` | 只发旋转包，本地视角不动 |
| 与同类模块互斥 | `:27-28` 开启时关闭 Derp / HeadRoll | 一致 |

无改动。

## 建议（未做）

- `tickCount % 100` 会取到 0..99，而原版 pitch 的合法范围是 ±90，91..99 超出上限
  （服务端会照单收下，其它客户端渲染时头部角度被夹住）。改成 `% 90` 或做一次 clamp 更严谨，
  但因为这是玩笑模块、且需要实机确认其它客户端表现，本次只记录不改。
- 同样没有平滑，20Hz 离散角度，属模块用途。
