状态：不适用

## 对照证据

参考侧有 `render/PopChams.kt`（图腾爆裂时的残影）。本工程 `hacks/PopChamsHack.java`（181 行）
用"监听音效包 + 画渐隐方片"实现。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 触发 | `:85-92` 监听 `ClientboundSoundPacket`，音效为 `TOTEM_USE` 时才处理 | 正确（1.20 里图腾爆裂没有专门的包，音效是最可靠的触发源） |
| 线程安全 | `:90` 用 `MC.execute(...)` 切回客户端线程再改 `pops` | **正确**（`PacketInputListener` 在收包线程触发，直接改列表会与渲染线程竞争；这里避开了） |
| 位置归属 | `:94-108` 找离音效位置 2 格内的玩家，用玩家的位置和身高 | 正确 |
| 过期清理 | `:111-117` 每 tick 按 `Duration` 清理 | 正确 |
| 换世界/关闭 | `:78-82`、`:68-76` 清空列表并摘四个监听器 | 正确 |
| GL 状态 | `:132-177` `try/finally` 里还原 `depthMask/enableDepthTest/disableBlend` | 正确 |
| 透明度 | `:146-150` 按存活时间线性衰减，`alpha < 0` 跳过 | 正确 |

无改动（本文件没有 GPL 头注释）。

## 建议（未做）

- `:115,146-148` 用 `System.currentTimeMillis()`（挂钟）而不是世界 tick 计时。挂钟在
  暂停/掉帧时也让残影按真实时间消失，属可接受选择（参考同样用挂钟）。未改。
