状态：不适用

## 对照证据

参考侧没有对应模块（参考没有"一键关掉所有功能"的紧急开关）。本项为自审，
全文读过 `hacks/PanicHack.java`（47 行）。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 一次关掉全部 | `:41-43` 遍历 `getAllHax()`，跳过自己，逐个 `setEnabled(false)` | 正常 |
| 关掉自己 | `:45` 最后 `setEnabled(false)` | 正常（不会把自己留成开启状态） |
| 不存档 / 不进 blocker | `:17` `@DontBlock` | 合理 |

无改动（`:44` 的缩进比同级多一个 Tab，纯排版，未动）。

## 建议（未做）

- 被关掉的模块若还需要"关闭时清理"（例如 AutoLibrarian 的破坏进度、Follow 的按键）
  各自 `onDisable` 会处理，Panic 不需要额外逻辑；目前没有发现被 Panic 关掉后残留状态的模块。
