状态：不适用

## 对照证据

参考侧没有对应模块（参考的 `Velocity.kt` 是取消受击击退）。本项为自审，全文读过
`hacks/AntiWaterPushHack.java`（86 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 取消水流推力 | `:68-73` `VelocityFromFluidListener` 里只取消"作用在玩家自己身上"的推力 | 正确 |
| 水中不减速（可选） | `:75-80` + `:82-85` `IsPlayerInWaterListener` 让原版以为玩家不在水里 | 由 `Prevent slowdown` 控制，默认关 |
| 水中起跳 | `:50-66` 需要同时满足"按住跳跃 + 落地 + 实际接触水（`isTouchingWaterBypass()`，绕开上面那个假的 inWater 判定）" | 正确，条件里特意用了 bypass 版本，避免自己的改写在同模块内互相干扰 |
| 关闭 | `:42-48` 三个监听器全部移除 | 正常 |

无改动。

## 建议（未做）

- `preventSlowdown` 打开时把"玩家在水中"整个改成 false，副作用包括水下挖掘速度、水下呼吸判定
  等也一起变化（注释里已提示"部分服务器视为加速作弊"）。属于该模块的固有取舍，未改。
