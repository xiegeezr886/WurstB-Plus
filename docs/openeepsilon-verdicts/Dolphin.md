状态：不适用

## 对照证据

参考侧的 `movement/FastSwim.kt` 是水平加速；本工程的 `Dolphin`/`Fish` 是垂直上浮，属于不同功能。
本项为自审，全文读过 `hacks/DolphinHack.java`（49 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 条件 | `:43-44` 必须在水中且没有按潜行 | 正常（按住潜行=想下沉） |
| 效果 | `:46-47` 每 tick `deltaMovement.y += 0.04` | 正常（原版水中上浮速度约 0.02~0.04/tick） |
| 与 Fish 互斥 | `:30` 开启时关闭 Fish；Fish 侧 `:30` 反向关闭 | 双向互斥，一致 |
| 关闭 | `:33-37` 摘监听（速度是瞬时量，无需还原） | 正常 |

无改动（`Fish` 的实现除 0.005 的增量外与它完全相同，是同一个模块的强弱两档）。
