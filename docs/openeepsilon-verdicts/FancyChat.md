状态：不适用

## 对照证据

参考侧没有 CHAT 类模块（模块目录只有 client / combat / misc / movement / player / render / setting，
共 110 个模块），没有 fancy chat / 全角聊天 / 反全角过滤之类的实现。本项为自审。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 转换范围 | `hacks/FancyChatHack.java:60`：`c < 0x21 \|\| c > 0x80` 原样保留 | 空格(0x20)、控制字符、非 ASCII 都不动，聊天换行不会被破坏 |
| 黑名单 | `:18,63-64` 保留 `(){}[]\|` | 不会把命令语法/权限符号变成全角 |
| 跳过命令 | `:42-43` `/` 或 `.` 开头不转换 | 正确（否则命令名会失效） |
| 转换方式 | `:66` `Character.toChars(c + 0xfee0)` | ASCII → 全角的标准偏移，字符码点合法 |

无改动。

## 建议（未做）

- `convertString`（`:49-56`）用字符串拼接逐字符累加（O(n²)），聊天消息长度上限 256，无实际影响。
- 转换后仍是合法 UTF-8 文本，不会被客户端自身的字符校验拦下（已确认 1.20.1 聊天包只做长度限制）。
