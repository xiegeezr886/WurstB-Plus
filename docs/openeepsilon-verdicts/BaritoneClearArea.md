状态：不适用

## 对照证据

参考侧没有对应模块（参考没有 Baritone 集成）。本项为自审，全文读过
`hacks/BaritoneClearAreaHack.java`（125 行）。

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 前置条件 | `:38-42` `canEnable()` 要求 Baritone 已加载且玩家存在 | 正确 |
| 选点 | `:82-98` 右键依次记下两个角点（第二次右键后不再覆盖） | 正确 |
| 只清一次 | `:75-79` `started` 标志保证两个角点齐全时只调用一次 `clearArea` | 正确 |
| 中途卸载 Baritone | `:69-73` 自动关闭模块 | 正确 |
| 关闭 | `:55-64` 摘三个监听器 + `BaritoneUtils.stop()` + 清角点 | 正确 |
| 视觉 | `:100-114` 两个角点各画一个 1/16 缩进的框，选好的区域画一个半透明大框 | 正常 |
| 名牌提示 | `:116-124` `[Clearing]` / `[Set corner 2]` | 正常 |
| 不存档 | `:20` `@DontSaveState` | 合理 |

无改动。

## 建议（未做）

- 该文件没有 GPL 头注释（`package` 在第 1 行），与 `BaritoneWalk` 一样；不知道作者归属，
  不擅自补版权声明。
