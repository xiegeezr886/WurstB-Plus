状态：已优化

## 对照证据

参考侧没有对应模块。本项为自审，全文读过 `hacks/InstantBunkerHack.java`（118 行）。

## 实际改动：恒真的多余判断

旧实现：

```java
		// build instantly
		if(startTimer <= 0)
		{
			for(BlockPos pos : positions)
				...
		}
```

**反例（Rule 9）**：方法开头 `:98-102` 已经 `if(startTimer > 0) { startTimer--; return; }`，
走到这里时 `startTimer` 必然 ≤ 0，所以这个条件**永远为真**，是纯粹的冗余判断（没有功能影响）。
已去掉这层 `if`，并保留原缩进内容，行为不变。

## 自审记录

| 关注点 | 位置 | 结论 |
| --- | --- | --- |
| 前置校验 | `:53-67` 必须落地、主手必须持方块，否则报错并自动关闭 | 正确（中文提示） |
| 材料提示 | `:69-70` `stack.getCount() < 57` 给警告 | **核对后确认 57 就是模板大小**：`:28-37` 的 `template` 数组按 `1 + 6*4 + 4*3 + 8*(2*3)` 数出来正好 57 项 |
| 位置计算 | `:72-81` 以玩家所在方块为基准，用朝向及其逆时针方向做旋转 | 正确（跟着朝向转，不会永远朝北建） |
| 只替换可替换方块 | `:108-110` `canBeReplaced()` + 不和玩家自身碰撞箱相交 | 正确（不会把自己封在方块里） |
| 放置 | `:110` `BlockPlacer.place(...)`，`:111` 挥臂一次 | 正常 |
| 收尾 | `:113-114` 落地后自动关闭 | 正确 |
| 与 Tunneller 互斥 | `:51` 开启时关闭 Tunneller | 合理 |

验证边界：只做了编译验证，没有实机建造验证。
